# Task 001: Project Scaffolding

## Status: Done

## Description
Create the project skeleton: package.json with Prism dependency, Dockerfile, docker-compose.yml, .gitignore, and plan documents.

## Acceptance Criteria
- [x] `npm install` succeeds
- [x] `npm start` attempts to launch Prism (will fail until spec exists)
- [x] `docker compose build` succeeds
- [x] `.gitignore` excludes node_modules
- [x] Plan directory structure in place
- [x] README.md with usage instructions, endpoint table, and contribution guide
